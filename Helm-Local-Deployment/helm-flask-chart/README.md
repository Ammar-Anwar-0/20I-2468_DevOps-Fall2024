# This is the folder which is created when you use the command "helm create helm-flask-chart".

### It contains 2 folder, charts and templates
### It contains 3 files, .helmignore, Chart.yaml, values.yaml
